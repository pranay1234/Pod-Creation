apiVersion: v1
kind: Pod
metadata:
    name: nodeapp
    labels:
        app: nodeapp
spec:
    containers:
        - name: nodeapp
          image: pranayreddya94/web:v1
          ports:
              - containerPort: 8080
